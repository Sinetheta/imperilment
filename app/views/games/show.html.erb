<%= show_for @game do |s| %>
  <%= s.attribute :ended_at %>
<% end %>

<%= link_to t(:edit), edit_game_path(@game), class: 'btn btn-default' %>
<%= link_to t(:back), games_path, class: 'btn btn-default' %>
