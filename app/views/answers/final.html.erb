<%= render 'nav' %>
<h1>Final Round</h1>
<div class='question-container row'>
  <div class='col-md-4'>
    <div class="area">
      <div class='category'><p><%= @answer.category.name %></p></div>
    </div>
  </div>
  <div class='col-md-8'>
    <h3>You may wager up to $<%= @game.score(current_user) %></h3>
    <% if @question && @question.errors.any? %>
      <div class="errors">
        <% @question.errors[:amount].each do |error| %>
          Wager <%= error %>
        <% end %>
      </div>
    <% end %>
    <form method='get'>
      <div class="form-group">
        <div class="input-group">
          <span class="input-group-addon">$</span>
          <input type='text' name='wager' class="form-control" />
        </div>
      </div>
      <div class="form-group">
        <input type='submit' class='btn btn-primary'/>
      </div>
    </form>
  </div>
</div>
