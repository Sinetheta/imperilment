<div class='pull-right'>
  <table class='breakdown'>
    <thead>
      <tr>
        <% @game.date_range.count.times do %>
          <th></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <tr>
        <% @game.all_answers.each do |answer| %>
          <td>
            <%= link_to [@game, answer] do %>
              <%= breakdown_icon status_for(answer) %>
            <% end %>
          </td>
        <% end %>
      </tr>
    </tbody>
  </table>
</div>
