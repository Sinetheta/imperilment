<h1><%= t(:editing_model, model: Question.model_name.human) %></h1>

<%= render 'form' %>

<%= link_to t(:show), [@game, @answer, @question], class: 'btn' %>
<%= link_to t(:back), [@game, @answer, Question], class: 'btn' %>
